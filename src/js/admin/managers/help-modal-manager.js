/**
 * @module HelpModalManager
 * @description Менеджер для управления модальным окном помощи/справки.
 *              На текущий момент выполняет роль инициализатора UI-компонента.
 *              Выделен в отдельный модуль для соблюдения единой архитектуры приложения
 *              и возможности легкого расширения в будущем (например, добавления
 *              специфичных обработчиков событий или аналитики).
 *
 * @requires HelpModal - UI-компонент модального окна справки
 */

import { HelpModal } from '../modals/help-modal.js';

export const HelpModalManager = {

    /**
     * Инициализация менеджера.
     * Точка входа, вызывается при загрузке страницы.
     * Делегирует инициализацию непосредственно UI-компоненту модального окна.
     */
    init() {
        HelpModal.init();
    },
};