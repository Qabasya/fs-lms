# BugFix
1. Добавь в разрешённые символы для создания пароля (в заявке apply проверка) эта: %*?!№#@
2. У учеников при подаче apply вместо логина передаётся почта (в Действия -> Изменить и в модалке заявке в поле логин находится почта - проверь проблема в сохранении данных и у нас потерялись логины или в том как модалка отображает данные)
2. Ослабь защиту формы apply: с одного ip в одно время может прилететь до 20 заявок. Либо добавь наш динамический айпи в белый список (мб первые октеты или как?) чтобы у нас очно могли 20 учеников подать заявки одновременно. Я часть лимитов уже поправил, остальное сам проанализируй. Может еще useragent проверять или как тут защититься от ботов?
3. На странице "Преподаватели" (https://future-step.ru/wp-admin/admin.php?page=fs_lms_userlist&tab=tab-4) давай группы одного предмета у преподавателя писать в одну строчку через запятую (а не в столбик как сейчас)
4. При переключении пунктов меню у меня на доли секунды появляется пустое пространство между заголовком и таблицей (например на странице https://future-step.ru/wp-admin/edit.php?post_type=inf_ege_courses). На локалке такого не было. В чем может быть проблема?
5. Верни в таблицу таксономий предмета слаг таксономии. Мне же он нужен для импорта легаси-заданий
6. И нужно при завершении заполнения форма apply скроллить страницу в самый верх

---

# План исправления

В списке выше два пункта «2.», поэтому ниже сквозная нумерация по порядку: Б1…Б7.

## Б0 (срочно). На проде не входят те, у кого в кеше старый фронтовый бандл

**Диагноз (подтверждён на проде 2026-09-14)**
- `FrontendAssets::enqueueFrontendBase()` версионирует `common.min.css`, `frontend.min.css`, `common.min.js` и
  `frontend.min.js` значением `$this->plugin_version`, а оно зашито в `BaseController::$plugin_version = '0.0.1'`
  и не меняется при деплое. В админке (`AdminAssets`) и в изолированных бандлах (`BundleLoader`) версия
  уже берётся из `filemtime()`.
- Beget отдаёт статику с `Cache-Control: max-age=604800` (7 дней). Адрес `frontend.min.js?ver=0.0.1` после деплоя
  остаётся прежним, поэтому браузер до недели берёт файл из кеша. На проде кешированная копия — 52 453 байта,
  без кода капчи входа (`login-form.js`); сервер по тому же адресу отдаёт новую, 53 067 байт.
- Старый бандл не перехватывает отправку формы входа и не запрашивает токен SmartCaptcha, форма уходит
  с пустым `captcha_token`, а `YandexSmartCaptchaProvider::validate()` пустой токен отклоняет сразу, без запроса
  к Яндексу. В итоге `?login=captcha` и «Не удалось пройти проверку». Капча реагирует не на конкретного человека:
  так откажут всем, кто заходил на сайт за неделю до деплоя. Так же может ломаться форма заявки, если её JS менялся.

**Шаги**
1. `FrontendAssets::enqueueFrontendBase()`: во всех четырёх вызовах заменить `$this->plugin_version`
   на `filemtime( $this->path( 'assets/…' ) )`, как в `AdminAssets::enqueueAdminBase()`.
2. Найти остальные места, где статика версионируется `plugin_version` без `filemtime`, и перевести их так же
   (фолбэки в настройках модулей — `file_exists() ? filemtime() : plugin_version` — оставить).
3. Задеплоить и проверить в браузере с «грязным» кешем: у `frontend.min.js` новый `?ver=`, вход проходит.
4. До деплоя обойти можно только очисткой кеша: Ctrl+Shift+R на `/sign-in/`.

**Статус: сделано** (шаги 1–2; других мест с постоянной версией у локальной статики нет). Осталось: деплой и шаг 3.

## Б1. Спецсимволы `%*?!№#@` в пароле заявки

**Диагноз**
- Клиент: у `fs_password` стоит `data-validate="latinOnly"` (`templates/frontend/apply-fields.php:239`),
  а `LatinOnlyValidator` пропускает только `[A-Za-z0-9_]`. Этот же валидатор висит на логине и на ключе
  предмета (`subject-modal.php`), поэтому расширять его нельзя.
- Сервер: `ApplicationCallbacks::ajaxCreateApplication()` читает пароль через `requireText()` →
  `sanitize_text_field()`, а она вырезает `%` вместе с двумя hex-цифрами после него: `Pass%12ab`
  сохранится как `Passab`, и ученик не войдёт. Состав и длину пароля сервер не проверяет вообще
  (как и логина).

**Шаги**
1. `src/js/common/validators/PasswordValidator.js`: `/^[A-Za-z0-9_%*?!№#@]+$/u`, сообщение
   «Разрешены латиница, цифры и символы _ % * ? ! № # @». Зарегистрировать ключом `password` в `validators/index.js`.
2. `apply-fields.php`: у `fs_password` поменять на `data-validate="password"`, у логина оставить `latinOnly`.
3. `ApplicationCallbacks::ajaxCreateApplication()`: пароль читать через `unslashRawString( 'password' )`
   с проверкой на пустоту, без `sanitize_text_field`.
4. Серверная проверка: завести DI-сервис `Services/Security/CredentialsPolicy`
   (`assertLogin()` = латиница/цифры/`_`, длина 3–20; `assertPassword()` = регэксп из п.1, длина 3–16 через `mb_strlen`),
   звать его в `ApplicationService::createApplication()` до шифрования. Ошибка — `DomainException` с тем же текстом,
   что в JS. Регэксп в JS снабдить комментарием со ссылкой на PHP-правило.
5. Прогнать всю цепочку: подача → зачисление → вход на `/sign-in/` с паролем вида `a%12*?!№#@`
   (`wp_signon` пароль не санитизирует). AdSync отдаёт пароль в `unicodePwd` (UTF-16LE), так что `№` допустим,
   но стоит проверить, что FGPP на OU учеников такие символы не отклоняет.
6. Заявки, уже поданные с `%XX` в пароле, восстановить нельзя. Если ученик скажет, что пароль не подходит, —
   перегенерировать пароль в карточке.

**Статус: сделано**, кроме проверки AdSync/FGPP в п.5. Правило проверяется в `ApplicationService::createApplication()`
**до** `verify()` кода: успешная проверка гасит код, и опечатка в пароле заставляла бы запрашивать новый.
Сквозная проверка на локалке: пароль `Ab%12*?!№#@` сохранился без изменений, `bad pass` отклонён.
Тесты — `tests/Unit/Services/Security/CredentialsPolicyTest.php`.

## Б2. Почта вместо логина в заявке

**Диагноз (уточнён при реализации 2026-09-14)**
Ошибок две, и логины действительно терялись.
1. Отображение: в `userlist-1-applications.php:344` стоял `data-login="<?php echo esc_attr( $studentEmail ); ?>"`,
   поэтому модалка «Изменить» (статус «ожидает родителя») показывала email.
2. Потеря данных: живые обработчики — `ApplicationDataCallbacks::ajaxUpdateApplicationData()` и
   `ajaxUpdateReviewData()` — пересобирали `StudentDataDTO` без `username` и `loginPassword`, а у этих
   параметров были значения по умолчанию `''`. **Любое сохранение заявки в админке (обе модалки) стирало логин
   и пароль**, после чего при зачислении `EnrollmentService::studentLogin()` брал email вместо логина, а пароль
   генерировался заново. Первичный анализ смотрел на `EnrollmentCallbacks.php`: это мёртвый код с устаревшими
   неймспейсами, который нигде не регистрируется, и там логин переносился правильно.
Ещё рядом: у полей «Логин» и «Пароль» в модалке есть карандаш, но `ApplicationModalManager::_handleSave()`
их не отправлял, а сервер не принимал, так что правка молча терялась.

**Статус: сделано.** Шаблон исправлен; у `StudentDataDTO` логин и пароль стали обязательными параметрами;
оба обработчика сохраняют их; правка логина и пароля в модалке работает (`resolveCredentials()`: пустое значение
оставляет прежнее, изменённое проверяется `CredentialsPolicy` и на занятость). Регрессия закрыта
`tests/Unit/Callbacks/Enrollment/ApplicationDataCallbacksTest.php`. Остались п.3 (проверка на проде) и п.4.
`EnrollmentCallbacks.php` стоит удалить отдельным коммитом.

**Шаги**
1. `userlist-1-applications.php:344` → `esc_attr( $sd['username'] ?? '' )`.
2. Правку логина и пароля в модалке рекомендую разрешить, раз интерфейс её предлагает:
   - `application-modal-manager.js`: отправлять `login` и `password`;
   - `EnrollmentCallbacks::ajaxUpdateApplicationData()`: логин проверять через `CredentialsPolicy::assertLogin()`
     и на уникальность через `UserManager::findByLogin()`, пароль читать через `unslashRawString` и проверять
     `assertPassword()`; пустое значение оставляет прежнее.
   - Если правка не нужна — убрать карандаши у этих двух полей и оставить их только для просмотра.
3. Проверить на проде, что логины действительно не потерялись:
   - вкладка «Заявки» после правки шаблона;
   - учётки учеников, у которых логин совпадает с email:
     ```sql
     SELECT u.ID, u.user_login, u.user_email
     FROM wp_users u
     JOIN wp_usermeta m ON m.user_id = u.ID AND m.meta_key = 'wp_capabilities'
     WHERE m.meta_value LIKE '%lms_student%' AND u.user_login LIKE '%@%';
     ```
     Если такие есть, это прежде всего ученики, чью заявку правили в админке до исправления (логин стёрся,
     а `studentLogin()` подставил email; пароль у них сгенерирован, а не тот, что они придумали). Реже —
     заявки до появления поля «Логин» или ветка `AccountProvisioningService::provisionStudent()`, где email уже
     занят и ученика привязывают к существующему пользователю. Логин в WP после создания не меняется, поэтому
     таким ученикам — сообщить логин (email) и пароль из карточки либо пересоздать учётку вручную.
4. Попутно нашлось, завести отдельными задачами после проверки:
   - уникальность логина сверяется только с WP-пользователями: две незачисленные заявки с одинаковым логином
     проходят, а вторая упадёт при зачислении на `wp_insert_user`;
   - если ученик указал почту родителя, `provisionParent()` найдёт по этому email учётку ученика,
     привяжет родителя к ней и сменит пароль;
   - `RecoveryService::resolveStuckEnrollments()` создаёт логин `student_{id}` со случайным паролем
     и не использует `username` из заявки.

## Б3. Лимиты формы apply (класс из 20 человек за одним IP)

**Диагноз.** Во что упирается очная подача:

| Счётчик | Где тратится | Лимит | Расход на ученика |
|---|---|---|---|
| `apply` (IP) | `ajaxSendOtpCode` **и** `ajaxCreateApplication`, плюс каждая переотправка кода и каждый неверный код | 20/ч | от 2 → блокировка примерно на 10-м ученике, причём уже на шаге ввода кода |
| `unamechk` (IP) | уход фокуса с поля логина и повтор при отправке (`apply-form.js`) | 20/ч | от 2 → примерно после 10-го проверка молча отключается: JS считает ошибку ответом «логин свободен» |
| `otpmail` (email) | отправка кода | 5/сутки | хватает |
| `parent` (IP) | анкета родителя по JOIN-ссылке | 3/ч | блокировка, если родители заполняют очно через один Wi-Fi |
| `join` (IP) | открытие JOIN-ссылки | 30/ч | хватает |
| form_token | `FormGuardService::MAX_TOKEN_AGE` = 1 ч | — | форма, открытая в начале урока и отправленная через час, отвечает «Обновите страницу» |

Кроме того, `ApplicationCallbacks` берёт сырой `$_SERVER['REMOTE_ADDR']`, а `LoginGuardController` —
`requestContext()->ip` с учётом `fs_lms_trusted_proxies`. Белый IP сработает, только если PHP видит настоящий
адрес клиента. **Проверено на проде 2026-09-14:** хостинг Beget, перед PHP стоит их nginx (`nginx-reuseport`),
CDN нет. В журнале «Авторизация» за день пять разных адресов у разных провайдеров, очные заявки пришли
с одного IP, онлайн-заявка — с другого. Значит, `REMOTE_ADDR` — настоящий IP клиента, прокси не мешает,
и `fs_lms_trusted_proxies` задавать не нужно.

**Решение по защите.** На сеть ставится белый (статический) IP. Для этого адреса все IP-лимиты увеличиваются
в фиксированное число раз, остальным посетителям остаются текущие значения. «Очный приём» и фильтр по User-Agent
не делаем. Капча, honeypot, метка времени, код на почту и лимит писем на адрес работают и для белого IP.

**Шаги**
1. `RateLimitService`: разделить счётчик на `allowOtpSend( $ip )` (ключ `apply_otp`, зовёт `ajaxSendOtpCode`)
   и `allowApplicationCreation( $ip )` (ключ `apply`, только `ajaxCreateApplication`). Базовые лимиты оставить
   прежними (20/ч): это исправление двойного счёта, а не ослабление защиты.
2. Белый IP — константа в `wp-config.php`, как остальные настройки окружения:
   `define( 'FS_LMS_TRUSTED_IPS', '1.2.3.4' );` (несколько адресов — через запятую). Читать через
   `PluginConfig::trustedIps(): array`, в интерфейс не выносить: адрес меняется редко, а опция в БД
   без необходимости расширяет, кто может ослабить защиту. Точный IP, без масок подсети.
3. `RateLimitService::check()` для IP-счётчиков (`apply_otp`, `apply`, `unamechk`, `emailchk`, `parent`, `join`):
   если адрес в `trustedIps()`, умножать лимит на `TRUSTED_IP_MULTIPLIER = 10`. Не обходить лимит полностью,
   чтобы заражённое устройство в сети не могло слать без ограничений.
   Запас для 20 учеников в час: код — до 5 отправок на ученика (упор в `otpmail` 5/сутки) → 100 из 200;
   создание — до 6 попыток (1 + неверные коды) → 120 из 200; анкеты родителей — 30 вместо 3.
   Лимиты по email (`otpmail`), по паре IP + пользователь (вход) и по пользователю (PII) не трогать:
   общий IP на них не влияет.
4. Брать IP из одного места: `ApplicationCallbacks` перевести с `$_SERVER['REMOTE_ADDR']` на
   `RequestContextProvider` (`requestContext()->ip`), как у входа. Сейчас результат тот же, но при смене хостинга
   или появлении CDN IP придётся настраивать в одном месте.
5. `apply-form.js`: запоминать последний проверенный логин и результат и не отправлять повторный запрос
   при отправке формы, если логин не менялся.
6. `FormGuardService::MAX_TOKEN_AGE` поднять до 4 ч: форму открывают в начале урока, а отправляют позже.
   С IP это не связано.
7. После установки белого IP проверить с устройства в этой сети: адрес на 2ip.ru совпадает с `FS_LMS_TRUSTED_IPS`
   и с IP собственного входа в журнале «Авторизация».

**Статус: шаги 1–6 сделаны**, тесты — в `RateLimitServiceTest` и `FormGuardServiceTest`. Для шага 7 после
установки белого IP нужно дописать в `wp-config.php` прода `define( 'FS_LMS_TRUSTED_IPS', '<адрес>' );`.

## Б4. Группы преподавателя в одну строку

**Диагноз.** В `userlist-4-teachers.php:115` группы одного предмета склеиваются через `<br>`.

**Шаги**
1. Заменить `implode( '<br>', … )` на `implode( ', ', … )`. Разделитель `<br><br>` между предметами оставить
   в обеих колонках, чтобы предмет и его группы стояли на одной высоте.
2. Длинный список групп переносится на следующую строку и сбивает выравнивание с колонкой «Предметы».
   Если это мешает, вывести пары одной колонкой в виде «Предмет — группа1, группа2».

**Статус: шаг 1 сделан.**

## Б5. Пустое место между заголовком и таблицей (только прод)

**Диагноз (подтверждён на проде 2026-09-14, список уроков `inf_ege_lessons`)**
- `src/js/admin/admin.js:59–69` через 5 с после `document.ready` гасит все
  `.notice-success, .notice-warning, .notice-info` цепочкой `fadeTo( 100, 0 )` → `slideUp( 100 )` → `remove()`.
- На проде в `#wpbody-content` есть скрытое уведомление стороннего плагина — `div#notice-corrupt-rest-api.hidden.notice.notice-warning`
  («Произошла непредвиденная сетевая ошибка! …REST API…»). Плагин показывает его только при сбое REST API.
- `fadeTo()` в jQuery 3 сначала вызывает `.show()` у скрытого элемента. Уведомление становится `display:block`
  с `opacity:0`: невидимый блок высотой 236 px выталкивает `form#posts-filter` с y=182 на y=432,
  затем `slideUp` за ~100 мс его схлопывает. Замер по кадрам: `display:none h=0` → `display:block h=236 opacity=0`
  (форма на 432) → элемент удалён (форма на 182). Layout Shift API фиксирует тот же сдвиг формы на 250 px
  и возврат за ~250 мс.
- Локально такого плагина нет, а видимые уведомления просто исчезают, поэтому эффект не воспроизводится.
- Заодно скрипт удаляет из DOM это чужое уведомление, и плагин уже не сможет показать ошибку REST API.
  То же происходит со всеми уведомлениями о предупреждениях и с информационными на любых экранах, где подключён `admin.js`.

Проверенные и отброшенные гипотезы: перенос уведомлений штатным `common.js` (к моменту пустоты шапка банка
уже стоит под заголовком) и View Transitions (сдвиг наступает через ~5 с после загрузки, а не при переходе).

**Шаги**
1. `admin.js`: гасить только видимые уведомления — после `.not( … )` добавить `.filter( ':visible' )`,
   а в исключения — `.hidden, .inline, [hidden]`.
2. Сузить автоскрытие до собственных флеш-сообщений: `#message`, `.updated`, `.notice-success.is-dismissible`.
   Чужие предупреждения и информационные уведомления (обновления, ошибки других плагинов) не трогать:
   их скрывает сам пользователь крестиком.
3. Проверить на проде: список уроков — через 5 с форма не прыгает (снова снять layout-shift в консоли,
   сдвига `form#posts-filter` быть не должно); после сохранения записи флеш-сообщение по-прежнему гаснет.
4. Необязательная оптимизация, к багу не относится — разбор ниже.

**Статус: шаги 1–2 сделаны.** Заодно автоскрытие перестало удалять `.inline`-блоки в модалках: предупреждение
в экспорте пакета (`bundle-export-modal.php`), в выгрузке ПД и на вкладке импорта через 5 с исчезали из DOM,
и открытая позже модалка оставалась без них. Шаг 3 — после деплоя.

### Почему список уроков такой тяжёлый

Прод, `edit.php?post_type=inf_ege_lessons`: 846 КБ HTML (140 КБ по сети), 116 скриптов, DOMContentLoaded
~1,7–2,4 с. Локально тот же экран — 442 КБ и 93 скрипта; разницу дают плагины прода (WooCommerce,
Rank Math, WPBakery, WP Mail SMTP и др.), их раскладку нужно снимать на проде отдельно.

Локальная раскладка — сравнение со штатным списком «Записи», где ассетов плагина нет (312 КБ, 63 скрипта):

1. **Ядро WP 7 — на любом экране админки**, от плагина не зависит: ~200 КБ встроенных переводов для палитры
   команд (`wp-block-editor` 140 КБ, `wp-components` 34 КБ, `wp-core-commands` 24 КБ) и ~32 КБ меню.
2. **Плагин добавляет +130 КБ HTML и +30 скриптов**, почти всё — на экране, где оно не используется:
   - `AdminAssets::enqueue()` зовёт `wp_enqueue_media()` на **всех** экранах плагина. Это медиатека целиком:
     `media-views`, `media-editor`, `media-audiovideo`, `media-models`, `plupload` + `moxiejs`, три файла
     `mediaelement`, `imgareaselect`, `image-edit`, `thickbox`, `media-upload`, `backbone`/`underscore`/`wp-backbone`/`wp-util`,
     `clipboard`, `jquery-ui-sortable`, `shortcode`, `mce-view`, плюс ~57 КБ HTML-шаблонов медиатеки
     (`<script type="text/html">`) и ~25 КБ их локализаций.
   - `AdminScreenContext::needsEditor()` истинно для уроков и курсов, в том числе на экранах **списков** →
     `wp_enqueue_editor()`: `editor`, `quicktags`, `wplink`, а на проде ещё и сам TinyMCE (`wp-tinymce.js` ~1,2 с
     в замере на списке курсов).
   - У `admin.min.js` жёсткие зависимости `wp-api` (+ `wp-api-request`, `backbone`), `editor` и `quicktags`:
     они тянутся на любой экран плагина, даже там, где `needsEditor()` ложно.
   - `admin.min.js` — один бандл на всю админку (236 КБ, в нём конструктор курса, модалки зачисления и т.д.).
     Встроенные данные — ~19 КБ (`fs_lms_vars` со всеми AJAX-экшенами и нонсами, а также `fs_lms_lesson_vars` и
     `fs_lms_task_editor_vars`, хотя список их не использует).
   - `admin.min.css` (88 КБ) зависит от стиля `wp-components`; Font Awesome — блокирующий CSS с `cdn.jsdelivr.net`.

Скрипты подключены в подвале синхронно: DOMContentLoaded ждёт загрузки и выполнения каждого, поэтому
на проде до готовности страницы проходит 1,7–2,4 с.

**Что можно сделать (по убыванию эффекта; проверять, что на экране не используются медиатека и `wp.editor`:
`BankRowActionsController::renderDraftCreatorModal`, `TaskCreationController`, `task-modal.js`)**
1. `wp_enqueue_media()` и `wp_enqueue_editor()` — только на `post.php`/`post-new.php` и страницах плагина,
   которым они нужны (конструктор курса, редактор шагов).
2. Убрать `wp-api`, `editor`, `quicktags` из зависимостей `admin.min.js`; подключать их на экранах, где они
   нужны.
3. Вынести тяжёлые разделы (конструктор курса) в отдельные бандлы через `BundleLoader`, как уже сделано
   для кабинета и плеера.
4. Локализовать `fs_lms_lesson_vars` / `fs_lms_task_editor_vars` только на экранах редактирования.
5. Font Awesome — локальной копией.

## Б6. Слаг в таблице таксономий предмета

**Диагноз.** Колонку «Ярлык (Slug)» убрали в `3ed34fa6` вместе с ручным вводом слага. Стиль
`.fs-table--taxonomy .column-slug` в `_tables.scss:148` остался.

**Шаги**
1. `subject-4-taxonomies.php`: вернуть `<th class="column-slug">Слаг</th>` после «Название», в ячейке —
   `render_fs_badge( $tax->slug, 'blue' )` (как было до `3ed34fa6`). В `tfoot` поменять `colspan` с 5 на 6.
2. Пересчитать ширины в `.fs-table--taxonomy`: 40/30/30 были рассчитаны на три колонки, теперь их шесть.
3. Необязательно: в `templates/admin/legacy-task-import.php` заменить текстовые поля таксономий
   автора/года/уровня на `<select>` из таксономий предмета, тогда слаг не придётся переписывать руками.

**Статус: шаги 1–2 сделаны** (ширины — утилитами `tw-25` / `tw-20` на заголовках, SCSS не менялся).

## Б7. Прокрутка вверх после отправки формы apply

**Диагноз.** `apply-form.js` переключает этапы классами (`showOtpStep()`, `showSuccess()`), а прокрутку не трогает.
На телефоне форма после отправки становится короче, и пользователь оказывается ниже карточки с полем для кода.
Ошибки сервера `showError()` вставляет в начало формы, и снизу их тоже не видно.

**Шаги**
1. В `showOtpStep()` и `showSuccess()` добавить `window.scrollTo( { top: 0, behavior: 'smooth' } )`
   (так же сделано в `components/scroll-top.js`).
2. В `showOtpStep()` после прокрутки ставить фокус на `#fs_otp_code` (`focus( { preventScroll: true } )`),
   чтобы на телефоне сразу открывалась цифровая клавиатура.
3. В `showError()` прокручивать к блоку ошибки (`scrollIntoView( { block: 'center', behavior: 'smooth' } )`).
4. Проверить в мобильной ширине оба перехода: форма → код и код → успех.

**Статус: шаги 1–3 сделаны**, шаг 4 — ручная проверка.

## Порядок работ

0. Б0 — первым и отдельным деплоем.
1. Б2 п.1, Б6, Б4, Б7 — правки в шаблонах и JS, по одной-две строки.
2. Б1 вместе с `CredentialsPolicy`, затем Б2 п.2 (правило общее).
3. Б3: шаги 1–6 (прокси проверен, не мешает), шаг 7 — после установки белого IP.
4. Б5: шаги 1–3, оптимизация из шага 4 — отдельно.
5. По результатам проверок: Б2 п.4.