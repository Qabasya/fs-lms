# FS LMS — Backlog замечаний по плееру и /profile/ (план реализации)

Источник — 8 замечаний по итогам Эпика 14 (плеер курса) и кабинета `/profile/`.
Первопричины подтверждены анализом кода (2026-07-04). Ниже — дословные
замечания, зафиксированные решения по развилкам и план по каждому пункту с
точками правки.

## Зафиксированные решения (2026-07-04)

- **D-1 (задача 1) — календарь индивидуальных занятий отдельный, не привязан к
  группе.** На одном календаре показываются ВСЕ индивидуальные занятия всех
  групп/учеников. Пример: занятие Вани (Группа 1) в среду 12:00 и Маши (Группа 2)
  в среду 14:00 — оба видны на одном календаре.
- **D-2 (задача 5) — dry-run покрывает всё, включая контрольные и экзамены.**
  Преподаватель/методист/админ должен иметь возможность прорешать в предпросмотре
  задания, работы И контрольные/экзамены. Результат нигде не сохраняется.
- **D-3 (задача 7) — сайдбар плеера при входе учителя не меняется вовсе.**
  Показывается полная штатная навигация кабинета (как в `/profile/`), без блока
  «Сейчас проходите» и без потери кнопки меню/выхода.
- **D-4 (задача 3b) — поведение блокировки урока по времени.** Пока до начала
  больше часа — статичное «Урок ещё не доступен». За час и ближе — «Занятие скоро
  начнётся» + таймер обратного отсчёта до начала; по достижении времени урок
  разблокируется.

---

## Исходные замечания (дословно)

1. По индивидуальным занятиям в КТП: я хотел чтобы их дизайн повторял дизайн
   обычных групповых занятий: слева сайдбар с темами, справа календарь с
   занятиями. Только в сайдбаре не темы курса, а уроки. Со всеми доступными
   уроками (сначала из курса, затем после разделителя все остальные), сверху —
   строка поиска урока по названию.
2. UI уроков в плеере: перемести кнопки навигации (далее и назад) наверх, чтобы
   они не прыгали в зависимости от высоты контента. Удали везде слова «Мгновенная
   проверка». Иконки шагов возьми ТОЧНО ТЕ же, что и в конструкторе, все 1 в 1,
   игнорируй дизайн из lms-course-player.
3. Нужно чтобы ученик мог перейти к текущему уроку (который в расписании на
   главной) прямо по нажатию на расписание. Плюс таймер: за час до начала —
   обратный отсчёт, при достижении времени страница обновляется, блокировка
   снимается.
4. Растягивай шаг с видеоплеером под всю высоту плеера (16 на 9 соотношение).
5. Дать преподавателю/методисту/админу при предпросмотре курса проходить задания,
   работы и контрольные, чтобы увидеть результат (без сохранения — пустой прогон).
6. Добавить к настройкам шага с задачами пункт «Отображать подсказку:» (рядом с
   «Попыток»): после скольких неправильных ответов показать подсказку (0 — сразу).
   Проверять, что число ошибок для подсказки всегда меньше числа попыток.
7. Из плеера курса у учителя нет выхода: в сайдбаре «Сейчас проходите», пропали
   Мои группы и Мои курсы. Не менять сайдбар вовсе.
8. Если отображается wpadminbar, половина `prof-side-user` скрыта за низом экрана.

---

## План реализации

### Задача 1 — Индивидуальные занятия в КТП: layout «сайдбар уроков + календарь»

**Что:** заменить плоский список индивидуальных занятий на layout как у групповых
(слева бар, справа календарь). В баре — уроки: сначала уроки курса, разделитель,
затем все остальные уроки предмета; сверху поиск по названию. Календарь —
**сквозной по всем индивидуальным занятиям** (D-1), не по одной группе.

**Файлы:**
- `src/js/profile/ktp.js` — весь UI (клиентский рендер).
  - Эталон layout: `renderKTP()` → `.prof-ktp-grid` (`.prof-theme-bank` + `.prof-kal`).
  - Удаляем/заменяем: `renderIndividual()` (плоский `.prof-indi-list`), поповер
    `openLessonPicker()`.
  - Переиспользуем логику бара: `renderLessonCandidates()` (поиск + «Уроки курса» /
    разделитель / «Все уроки предмета») уже написана — поднять в левый сайдбар.
- Бэкенд (готово, менять минимально):
  - `ScheduleService::lessonCandidatesForGroup()` → `LessonManager::getBankBySubject()`.
  - `ScheduleService::getIndividualProgram()` — слоты.
- **Новое:** нужен сквозной сбор индивидуальных занятий по всем группам для одного
  календаря (сейчас `loadIndividual()` уже фанится по группам — переиспользовать
  сбор, но рендерить в календарную сетку, а не в список). Правый календарь для
  индив-режима строит собственную сетку по датам занятий (по `scheduled_at`
  слотов), т.к. общей `meetings`-сетки у сквозного индив-календаря нет.

**Объём:** M.

### Задача 2 — UI плеера

**2a. Кнопки «Далее/Назад» наверх.**
- `templates/frontend/lesson-player/player.php` — перенести блок `.cnav#fsNav`
  (сейчас `:272`, после `.steproot`) выше `.steproot`.
- `src/scss/player/components/_strip.scss:113` — `.cnav`: `margin-top`→`margin-bottom`.
- JS не трогаем (`core.js` вяжется по ID).

**2b. Убрать «Мгновенная проверка».**
- `templates/frontend/lesson-player/partials/step-task.php:28` — удалить `<span>·
  мгновенная проверка</span>`.
- `src/js/player/step-task.js:130` — убрать формулировку «Проверено мгновенно» из
  вердикта.

**2c. Иконки шагов 1-в-1 с конструктором.**
- Источник истины: `src/js/admin/services/step-editor.js` — карта `ICON` (`:24-30`)
  + маппинг StepType→ui (`:39-43`).
- Плеер: `src/js/player/icons.js` `typeIco()` (`:19-52`) — переписать, чтобы отдавал
  те же пути. Потребители (`strip.js`, `rail.js`) не трогаем.
- Учесть: viewBox 24 vs 20, покраска через аргумент `color` vs `currentColor`.

**Объём:** 2a — S, 2b — XS, 2c — S/M.

### Задача 3 — Переход к уроку из расписания + блокировка с таймером

**3a. Клик по расписанию → плеер.**
- `src/js/profile/learner.js` `schedRow()` (`:176`) — обернуть строку в
  `<a href="${player_url}">` (данные `player_url`/`status` уже есть в модели).
  Паттерн — `lessonRow()` (`:214`).

**3b. Блокировка + таймер (D-4).**
- Гейт: `LessonGateResolver::dateReached()` (`scheduled_at <= now`).
- Экран блокировки: `templates/frontend/lesson-player/locked.php` (рендерится из
  `LessonPlayerController.php:75`). Пробросить `scheduled_at` (в scope есть
  `$row->scheduledAt`).
- Логика показа:
  - `now < scheduled_at − 1ч` → статично «Урок ещё не доступен».
  - `scheduled_at − 1ч ≤ now < scheduled_at` → «Занятие скоро начнётся» + JS-таймер
    обратного отсчёта; по достижении `scheduled_at` — авто-reload (гейт снимется).
- Новый маленький JS для таймера (в бандл плеера или отдельный inline-безопасный
  модуль под `src/js/`).

**Объём:** 3a — S, 3b — M.

### Задача 4 — Видео-шаг на всю высоту плеера (16:9)

- `templates/frontend/lesson-player/partials/step-video.php` — вариант карточки
  без паддинга для видео (full-bleed).
- `src/scss/player/components/_step-video.scss:7` — ограничить `.vp` по высоте
  `.cscroll`/вьюпорта, чтобы `aspect-ratio:16/9` считался от высоты; добавить
  модификатор `.card16` для видео.

**Объём:** S/M (SCSS).

### Задача 5 — Интерактивный предпросмотр (dry-run: задания + работы + контрольные)

**Что:** в preview превьюер решает задания/работы/контрольные, видит вердикт,
ничего не сохраняется (D-2).

**Файлы:**
- Снять обнуление в preview: `CoursePreviewService::renderTaskData()` (`:155-181`),
  `renderWorkData()` (`:186-221`).
- Снять клиентские заглушки: `src/js/player/step-task.js:53,88`,
  `step-work.js:122`; включить кнопки в партиалах (`step-task.php:88`,
  `step-work.php:74`, `step-assessment.php:79`) под флагом dry-run.
- **Задания:** ветка no-persist в `SubmitTaskAnswerCallbacks` — `checker->check()`
  (`:127`) без `group_lesson_id`/лимита/`taskAttempts->create`/`progress->mark*`.
- **Работы:** grade-only путь мимо `SubmissionService::submitBatch`
  (переиспользовать `BatchCheckService`) — вернуть `per_task` без записи.
- **Контрольные/экзамены:** no-persist attempt-runner в обход
  `AssessmentAccessPolicy::canAccess` (`AttemptService.php:41`); grade-only через
  `AttemptResultService`. Самая тяжёлая часть.
- Нонсы решения на preview-роуте уже прокинуты (`Enqueue.php:345`).

**Объём:** L.

### Задача 6 — Настройка «Отображать подсказку: после N ошибок»

**Что:** рядом с «Попыток» — число «показать подсказку после N ошибок» (0=сразу),
валидация N < попыток. Инфраструктура почти готова:
`StepSettingsDTO::hintAfterErrors` (ключ `hint_after_errors`), раскрытие в
`LessonPlayerService.php:235` / `SubmitTaskAnswerCallbacks.php:149`, плеер
раскрывает (`step-task.js:119`), поле текста `task_hint` есть.

**Гэпы:**
1. `LessonCallbacks::sanitizeStep` (`:227-229`) — whitelist только `max_attempts`;
   добавить `hint_after_errors` (клампить `< max_attempts`), иначе стирается при
   сохранении.
2. UI конструктора — `src/js/admin/services/step-editor.js:626` (блок
   `.fs-cb-step-attempts`): добавить number-input → `step.payload.settings.
   hint_after_errors`, валидация N < max_attempts (при `max_attempts=0`=∞ — любой N).

**Объём:** S.

### Задача 7 — Полная навигация в сайдбаре плеера для учителя (D-3)

**Что:** при входе учителя (preview) сайдбар плеера показывает штатную навигацию
кабинета целиком, без блока «Сейчас проходите», с рабочим меню/выходом.

**Файлы:**
- Плеер — отдельный full-HTML документ: `templates/frontend/lesson-player/player.php`.
  - Сайдбар `:112-166`, `$nav_items` `:64-69`, блок «Сейчас проходите» `:137-155`.
  - Флаг `$is_preview` (`:34`).
- В preview рендерить полную профильную навигацию (роль-зависимую) вместо
  ученической + скрыть «Сейчас проходите». Источник пунктов навигации —
  профильный конфиг (сверить с `src/js/profile/app.js buildSidebar()` /
  `ProfileController`); ссылки ведут в `/profile/`.

**Объём:** S/M.

### Задача 8 — wpadminbar перекрывает `prof-side-user`

**Причина:** `templates/frontend/profile.php:28` хардкодит `<body
class="fs-profile-page">` без `body_class()` → нет класса `admin-bar` → правило
`body.admin-bar .prof-app { height: calc(100vh - 32px) }` (`_layout.scss:16`) не
матчится, а админ-бар всё равно сдвигает `100vh` вниз.

**Фикс:** `profile.php:28` — добавить `body_class()` (или класс `admin-bar`).
Лучше — перейти на `--wp-admin--admin-bar--height` в `_layout.scss` вместо
хардкода 32/46px.

**Объём:** XS.

---

## Статус — всё реализовано (2026-07-04)

Все 8 замечаний закрыты. Сборка зелёная, контейнер поднимается (HTTP 200, без
фаталов), `LessonCallbacks`-тесты проходят.

- **№8** — `profile.php` → `body_class()` (класс `admin-bar` активирует уже
  существующее правило `_layout.scss`).
- **№2b** — снят `step-task.php:28` + вердикт `step-task.js`.
- **№6** — `hint_after_errors` в `sanitizeStep` (кламп `< max_attempts`) + поле в
  `step-editor.js` (валидация N<попыток). Раскрытие/DTO/плеер уже были готовы.
- **№3a** — `learner.js schedRow()` обёрнут в `<a href=player_url>`.
- **№2a** — `.cnav` перенесён над `.steproot` в `player.php` + margin в `_strip.scss`.
- **№2c** — `player/icons.js typeIco()` отдаёт глифы конструктора (`step-editor.js ICON`).
- **№4** — «кинорежим» видео: `_step-video.scss` + `--video-h-chrome` +
  `container-type: size` на `.content` (16:9 от высоты области контента).
- **№7** — в preview `player.php` рендерит штатную навигацию кабинета из
  `shell['nav']` (`CoursePreviewController::previewNav`), «Сейчас проходите»
  скрыт, выход ведёт в `/profile/`.
- **№3b** — `locked.php` три состояния (D-4) + `lesson-countdown.js`
  (frontend-бандл), авто-reload на старте.
- **№1** — `ktp.js`: индив-режим = сайдбар уроков (`renderLessonCandidates`) +
  сквозной календарь всех инд-занятий (D-1); клик по слоту → назначение урока.
- **№5** — dry-run проверка (D-2): новый `PreviewSolveController`/
  `PreviewSolveCallbacks` (гейт `AuthorLmsCourses`, без сохранения) для заданий
  (`checker->check`), работ и контрольных (`BatchCheckService::check`); preview
  рендерит контрольную инлайном как работу (`CoursePreviewService::
  renderAssessmentData` + `step-assessment.php`), драйвит `step-work.js`.

### Заметки

- **Concurrent-fork инцидент:** фоновый fork-агент (разведка по контрольным)
  вопреки инструкции «read-only» параллельно правил файлы (дубли enum-кейсов,
  посторонняя правка `ProblemsController`). Всё вычищено; на будущее — для
  разведки во время правок использовать read-only `Explore`, не `fork`.
- **Пред-существующие падения тестов (не связаны с этими правками):** 57 ошибок +
  2 фейла из-за рассинхрона конструкторов (Jul 3) с тестами — `ScheduleService`,
  `LessonPlayerService`, `CourseBuilderCallbacks`, `ProfileViewResolver`,
  `RoomCallbacks`, `DashboardService`. Рекомендуется отдельный проход синхронизации
  тестов.
- **Follow-up:** покрыть `PreviewSolveCallbacks` PHPUnit-тестом (по [[cover-callbacks-with-tests]]).

Сборка: `npx gulp build` после правок JS/SCSS. После PHP — при необходимости
`docker restart wp_app` (OPcache).


Новые баги:
1. Исправить profGradePop - упирается в край экрана и контент обрезан - ширина не менее 500, сдвиг left пересмотреть (должно быть не в пикселях, а относительно экрана)
2. Добавь возможность изменять и добавлять индивидуальные занятия прямо на календаре в КТП. Можно ткнуть в любой день и вылезет то же самое окно с индивидуальным занятием - выставляется ФИО ученика, время, кабинет и тема (дата фиксирована из календаря). Аналогично второй вариант - утсановка индивидуального занятия из меню Группы (те же поля, но фиксировано ФИО ученика, а не дата). В обоих случаях одно и тоже окно, но фиксируются разные поля.
3. Видео-шаг. Это должна быть такая же карточка, как у остальных шагов. Только внутри карточки подгружается видео с разрешением сторон 16 на 9. Пока видео не подгружено (у нас с s3 хранилища = изначально нет видео) - надпись "Видео скоро появится"
4. Сделай шаги разной ширины в зависимости от основного сайдбара (s-side). То есть 2 размера для контейнера с шагом внутри контента (col) (на 4к экране это 1200 пикселей с сайдбаром и 1400 без - оптимизируй под разные экраны, в т.ч. и мобильные вертикальные)
5. Почему в Банке задач все задачи кроме 16673 можно удалить, а эту только в архив. Если все одинаково используются в курсе, то нужно чтобы все могли быть только перемещены в архив же. Причем, что будет с этими задачами внутри курса? Как это будет отмечено? Аналогично, что будет если я удалю контрольную которая уже в курсе? Как будто не стоит давать возможность удаления, пока существует использование в курсе.

---

## Новые баги — разбор (2026-07-04)

Первопричины подтверждены read-only анализом кода.

**Решения (2026-07-04):**
- **B2** — «тема» в окне инд. занятия = **выбор банковского урока** (`lesson_id`,
  через `lessonCandidates`), не free-text-подпись.
- **B3** — изначально url видео **пустой** (ссылки нет, пока не загружено) →
  `mode='none'` → «Видео скоро появится». JS-`error`-обработчик не нужен.
- **B5** — **удаление реально работает** даже для урока/работы в ОПУБЛИКОВАННОМ
  курсе (баг): гард покрывает только задачи (problems), не lessons/works/
  assessments. Нужно распространить запрет на все типы + пометка архивных в курсе.

### B1 — profGradePop: ширина ≥ 500, клэмп относительно экрана — **XS**

**Причина:** ширина захардкожена в двух местах, и клэмп по горизонтали использует
устаревшую константу 188. Широкий контент (форма инд. занятия `openIndiForm`) не
влезает в 188px → уезжает за правый край и режется.
- `src/scss/profile/components/_overlays.scss:50` — `width: 188px;` → заменить на
  `min-width: 500px; max-width: calc(100vw - 20px);` (оставить `overflow-y:auto`,
  `max-height` из :51).
- `src/js/profile/utils.js:202` — `const pw = 188;` → `const pw = pop.offsetWidth;`
  (`.open` уже добавлен на :200, ширина измерима). Клэмп на :204
  (`Math.min(left, window.innerWidth - pw - 10)`) тогда сам держит бокс на экране;
  центрирование :203 и `Math.max(10, …)` уже делают left относительным.

Делать вместе/перед B2 — это та же форма.

### B2 — Индивидуальные занятия: создание/редактирование из календаря и «Группы» (одно окно) — **L** — ✅ ГОТОВО

**Реализовано:**
- Общая модалка `src/js/profile/indi-modal.js` (`openIndiModal`) — создание и правка.
  Поля: группа / ученик / дата / время / кабинет / тема. Фиксируются по контексту:
  «Группы» → группа+ученик; календарь КТП → дата ячейки; правка → группа. **Тема =
  выбор банк-урока (`lesson_id`)** — развилка ниже закрыта в пользу урока (решение B2).
- «Группы»: `openIndiForm` теперь тонкий враппер над `openIndiModal` (group+student fixed).
- Календарь КТП (`ktp.js`): клик по свободному месту дня → добавить (дата фикс.);
  ✎ на слоте → правка; пустой экран → кнопка «+ Добавить занятие». Сайдбар-назначение
  урока (НБ-9) сохранён (клик по слоту) — B2 добавлен поверх, ничего не сломано.
- Бэкенд правки: `GroupLessonRepository::setStudentPersonId` + `ScheduleService::
  updateIndividualLesson(glid, scheduledAt?, roomId?, studentPersonId?, lessonId?, actor)`
  + `AjaxHook::UpdateIndividualLesson` + `ProgramCallbacks::ajaxUpdateIndividualLesson`
  + регистрация в `ScheduleController`. `getIndividualProgram` отдаёт `room_id` (префилл).
- Конфиг: блоки `schedule` и `roster` дополнены недостающими действиями
  (getRoster / getFreeRooms / lessonCandidates / createIndividual / updateIndividual),
  чтобы api любого экрана покрывал модалку.
- Проверено: сборка + eslint чисто, PHPUnit 741/0/0, boot 200.

**Суть (исходно):** одно окно (ФИО ученика / время / кабинет / тема). Из календаря КТП —
клик по любому дню → окно с ФИКС. датой (ученик/время/кабинет/тема правятся). Из
«Группы» — окно с ФИКС. учеником (дата/время/кабинет/тема правятся).

**Что уже есть:**
- Окно = `openIndiForm(pid, anchor)` — `src/js/profile/groups.js:102-158`
  (рендерит в `profGradePop`, ученик фикс). Уже создаёт занятие.
- Бэкенд создания: `ScheduleService::createIndividualLesson(groupId, studentPersonId,
  scheduledAt, endsAt?, lessonId?, label?, teacherUserId?, actorUserId, roomId?)`
  (`inc/Services/Group/ScheduleService.php:67`) → `ProgramCallbacks::
  ajaxCreateIndividualLesson()` (:313) → `AjaxHook::CreateIndividualLesson`.
- Действия `createIndividual` + `getFreeRooms` — только в блоке `roster` конфига
  (`ProfileViewResolver.php:237-238`), НЕ в `schedule` (который использует ktp.js).
- Пикеры: `openStudentPicker` (`picker.js:54`), roster через `getRoster`.

**Что делать:**
1. Вынести `openIndiForm` → `src/js/profile/indi-modal.js` как
   `openIndiModal({api, groupId, roster, fixed:{studentPid?|date?}, prefill?, onSaved})`.
   «Группы» зовёт с `fixed.studentPid`, КТП — с `fixed.date`.
2. Клик по `.kal-cell[data-day]` в `renderIndiCalendar` (`ktp.js:447/455`, сейчас
   обработчик только на `.indi-slot`) → открыть модалку с фикс. датой.
3. Прокинуть `createIndividual` + `getFreeRooms` в блок `schedule`
   (`ProfileViewResolver.php:200-213`, нонс тот же `SaveSchedule`).
4. Из кросс-группового календаря нужно выбрать группу+ученика → пикер группы +
   `openStudentPicker` + roster.
5. **Редактирование (сейчас нет общего апдейта):** добавить
   `ScheduleService::updateIndividualLesson(glid, scheduledAt?, roomId?,
   studentPersonId?, label?, actorUserId)` (композиция `updateSchedule` :88 +
   `setRoom` :198 + **новые** репо-примитивы `setLabel`/`setStudentPersonId`) +
   `AjaxHook::UpdateIndividualLesson` + `ajaxUpdateIndividualLesson` (зеркало :313)
   + регистрация в `ScheduleController` + action `updateIndividual`.

**Развилка:** «тема» = free-text `label` (как сейчас) ИЛИ банковский урок
(`lesson_id`)? Назначение банк-урока сейчас — отдельный flow (сайдбар КТП
`assignLesson`). Уточнить.

### B3 — Видео-шаг: обычная карточка + «Видео скоро появится» (отменяет «кинорежим» из №4) — **S**

**Суть:** видео-шаг = такая же `.card16`, как у остальных шагов, с 16:9-видео
внутри; пока видео нет (S3 не залит) — надпись «Видео скоро появится».
1. Удалить «кинорежим»: блок `src/scss/player/components/_step-video.scss:24-42`
   (прозрачная карточка `background:transparent;border:none;padding:0` +
   `max-width: min(100%, (100vh − …))`). Базовый `.vp` (`:7-22`, `aspect-ratio:16/9`)
   внутри нормальной `.card16` уже даёт 16:9 по ширине.
2. Удалить мёртвый токен `--video-h-chrome` (`_variables.scss:81`).
3. Плейсхолдер: пустой url → `mode='none'` (`StepContentRenderer::resolveVideoMode`
   :257-266). В `step-video.php` добавить ветку `elseif ( 'none' === $video_mode )`
   (после `:95`, слот-ветки) → `<p class="step-muted">Видео скоро появится</p>`.

**Caveat:** если в БД уже лежит S3-URL, а объект ещё не залит (`mode='native'`, но
404) — плейсхолдер по пустому url не сработает; нужен JS-`error` на `<video>`
(в `step-video.js` его нет). Уточнить, как S3-flow ставит url (если изначально
пусто — ветка `none` покрывает).

### B4 — Адаптивная ширина шага (`.col`) от сайдбара и экрана — **S/M**

**Суть:** `.col` шире при скрытом сайдбаре; на 4K ~1200 с сайдбаром / ~1400 без;
адаптив для меньших экранов, вкл. мобильный-вертикальный.

Сворачивание сайдбара = класс `body.menu-off` (`shell.js:17`; `.app` grid
`244px 1fr` → `body.menu-off .app { 0 1fr }`). Сейчас `--colw:900px` фикс; в
player-SCSS **нет ни одного `@media`**.
- `_variables.scss:76` — база `--colw: 1200px`.
- `_shell.scss` (у `:17`) — `body.menu-off { --colw: 1400px; }` (гейт на широкие
  экраны, напр. `@media (min-width:1900px)`).
- `@media (max-width:1024px){ :root{ --colw:100% } }` + ужать `.player` padding
  (`:337`) и `.content` margin-left (`:365`).
- Мобильный: `@media (max-width:640px)` — свернуть жёлоба, уменьшить `.card16`
  padding (`_card.scss:11`). Рассмотреть `--colw: min(1200px, 100%)`.

### B5 — Удаление vs архив банк-контента при использовании в курсе — **M (в основном верификация + пометка)**

**Реальность:** `ContentDeletionGuard` (`inc/Controllers/Subject/
ContentDeletionGuard.php`) **уже** запрещает физическое удаление (`pre_trash_post`,
`pre_delete_post`) при `usage>0` — только «В архив»; orphan (usage 0) удаляется
штатно. 16673 архив-онли, потому что это ЕДИНСТВЕННАЯ задача, реально используемая
в курсе; остальные — usage 0 → удаляемы. **Гард работает как просит пользователь.**

Настоящие задачи:
1. Проверить, что `ContentUsageService::usageCount()` (`inc/Services/Course/
   ContentUsageService.php:54`) ловит ВСЕ типы ссылок: задача в task-шаге, задача
   в работе, задача в контрольной, работа/контрольная/урок в курсе — иначе часть
   «используемых» ошибочно удаляема (возможная причина, почему у пользователя
   защищена только 16673).
2. Убедиться, что гард применяется ко всем банк-типам (problems, works,
   assessments, lessons), не только к задачам.
3. Как помечается заархивированный контент ВНУТРИ курса (шаг курса всё ещё
   ссылается на archived-пост)? Добавить индикатор «в архиве» на шаг в
   конструкторе/плеере вместо «битой» ссылки.

**Развилка:** чего хочет пользователь сверх текущего гарда — (а) просто
подтвердить, что удаления нет при использовании (уже так), (б) пометка
archived-в-курсе, (в) расширить детект usage. Уточнить.

**Порядок:** B1 (быстрый, разблокирует форму B2) → B3 → B4 → B5 → B2 (самый крупный).