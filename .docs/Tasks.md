1. Убери эффект hover на таблицах, но строке-заголовку сделай цвет фона rgb(238, 240, 244)
2. Сделай на страницах с таблицам Ученики и Родители все фильтры шириной 310 пикселей (и фио, и предметы и группы)
3. В таблице Ученики в колонке ФИО ученика не отображается отчество ученика. Сделай чтобы отображалось (как в таблице Родители)
4. Я добавил ссылку записи на консультацию в настройки, но она не сохраняется. Выведи лучше её из блока "тестовое окружение" и добавь в Оформление. И чтобы она сохранялась по нажатию "Сохранить настройки"
5. Всё еще не работает импорт 19 заданий. Код вставляется, а три условия - нет. Проверь чтобы из файла /Users/daniil/FS-LMS/wp-content/plugins/fs-lms/.docs/legacy_tasks_import/legacy_tasks_import_task_19.json корректно импортировалось 22 задания по три части текста (19, 20 и 21 задания). Импортирую я их в задание номер 19 (у него тип "Связка 19-21"). Заодно поправь сам файл json, чтобы он корректно разделялся на 3 задания и каждое было подписано жирным шрифтом (Задание 19, Задание 20, Задание 21)
6. latex не корректно работает в шаге Лекция. Ощущение, что слеши \ обрезаются (например \cdot выглядит как cdot)
7. Картинки в лекциях игнорируют выравнивание по центру (на них действует тег p и они выравниваются как текст по левому краю). Давай в целом сделаем по умолчанию выравнивание текста по всей ширине, а картинок по центру
8. Таблицы fs-table-center, которые вставляются в заданиях в тренажёре сделай по ширине на 90% контейнера и чтобы столбцы равномерно распределялись по всей ширине таблицы
9. Мобильная версия Тренажёра: фильтры должны быть вверху страницы, кнопки "Показать ответ" и "Смотреть решение" расположить по центру блока друг под другом
10. Мобильная версия одного задания: увеличить шрифт заголовка до 32 пикселей (или какое там ближайшее значение из токенов)
11. Если есть возможность то в мобильной версии страницы одной статьи текст выровнять по ширине и сделать переносы слов (если нет такого, то просто выровнять по ширине без переносов)

---

# План выполнения

Порядок: сначала высокий приоритет (8 → 1 → 5), затем обычный (4 → 3 → 2 → 6 → 7), в конце низкий (9 → 11 → 10).
Задачи 8 и 1 правят один миксин — делаются одним заходом.

## Этап 1. Высокий приоритет

### Шаг 1. Задачи 8 + 1 — таблицы контента (`src/scss/shared/_content-tables.scss`)
Обе задачи касаются общего миксина таблиц из редактора: он подключён в карточке тренажёра, на странице задания, в статье, в шаге урока и в `.wpb_wrapper`.
1. **(8)** В `fs-content-table-parts` добавить к `&.fs-table-center`: `width: 90%` (перебивает `fit-content` из `fs-content-table-box`) и `table-layout: fixed`, чтобы колонки делились поровну. Проблема: у таблицы `display: block`, поэтому `table-layout` не сработает. Для `.fs-table-center` вернуть `display: table` и `margin-inline: auto`, а горизонтальную прокрутку на мобильных сохранить через `max-width: 100%` + `overflow-wrap: anywhere` в ячейках. Проверить на узкой кодовой таблице и на широкой (8+ колонок).
2. По задаче написано «в тренажёре». Уточнить: оставить правило общим для всех мест (так таблица везде выглядит одинаково, об этом говорит комментарий в миксине) или ограничить `.tcr-condition` / `.fs-task-condition`. По умолчанию — общее.
3. **(1)** Удалить `tbody tr { transition }` и `tbody tr:hover`.
4. **(1)** Фон шапки `th` = `rgb(238, 240, 244)`. Это `#eef0f4`, в токенах уже есть `t.$line-2`. Нужен семантический токен поверхности: завести `$surface-head: #eef0f4` в `shared/_tokens.scss` и `--surface-head` в `shared/cabinet/_theme.scss`. Сырой `line-2` как фон не использовать.
5. **(1)** Таблица без `<thead>` (вставлена из Word, у первой строки `td`) останется без цветной шапки. Уточнить, надо ли красить первую строку `tbody` при отсутствии `thead`. По умолчанию — нет.
6. `npx gulp styles:frontend` + `npm run lint:css`; проверить тренажёр, страницу задания, статью, шаг «Лекция».

### Шаг 2. Задача 5 — импорт связки 19-21
Файлы: `inc/Services/Task/LegacyTaskImportService.php`, `inc/DTO/Task/LegacyTaskRowDTO.php`, `.docs/legacy_tasks_import/legacy_tasks_import_task_19.json`.
JSON уже в формате `subparts` {19,20,21}, все 22 записи с тремя условиями. Сервис пишет их в `task_{N}_condition`, и это совпадает с полями `ThreeInOneTemplate`. Значит, ломается не разбор. Кандидаты по порядку проверки:
1. **Дедуп.** `alreadyImported()` пропускает строки, у которых `legacy_number` уже импортирован. Если 19-е заданий заливали до поддержки `subparts` (тогда пришёл только код), повторный импорт молча пропускает все 22 строки с предупреждением. Проверить отчёт импорта и мету `LegacyImportNumber` в БД. Лечение: удалить старые посты или добавить в форму режим «дозаполнить существующие» (fillContent по найденному посту вместо skip).
2. **Children связки не материализуются.** `MetaBoxController` на сохранении Triple вызывает `taskBundles->syncChildren()`, импорт этого не делает. В тренажёре и на фронте подзадания могут быть пустыми, пока пост не пересохранят. Добавить `syncChildren($postId)` в `importBatch()` после `fillContent()`, если шаблон номера — `TaskTemplate::Triple`. Зависимость — через конструктор.
3. **Слэши.** `PostManager::updateMeta()` зовёт `update_post_meta()` без `wp_slash()`, а ядро само делает `wp_unslash`. Все `\` в условиях и в коде (`'\n'` в Python) срезаются. Лечится вместе с задачей 6 (шаг 7).
4. **Шаблон номера.** Проверить, что номеру 19 предмета действительно назначен `triple_task`. Иначе `fieldMismatchWarning()` выдаёт предупреждение, но условия в метабоксе не видны.
5. Правка JSON (скриптом, не вручную): каждое `subparts.{N}.condition_html` начинается с `<p><strong>Задание N</strong></p>`, переводы строк `\n` превращаются в абзацы `<p>`. Перед этим проверить, не выводит ли фронт связки свои подписи «№19/20/21», чтобы не получить двойной заголовок (`TaskMetaService::getCombinedCondition()`, шаблон страницы задания, `task-card.js`). Если выводит — жирную подпись в JSON не добавлять и согласовать с пользователем.
6. Тесты: `LegacyTaskImportServiceTest` — Triple-строка даёт три `task_{N}_condition` и вызывает `syncChildren`. Прогнать реальный импорт 22 записей в номер 19 на dev, проверить метабокс, тренажёр и страницу задания.

## Этап 2. Обычный приоритет

### Шаг 3. Задача 4 — ссылка на консультацию не сохраняется
Причина: `src/js/admin/services/settings/config-settings.js` (около стр. 47) отправляет только `test_env`, `otp_bypass_code`, `brand_logo_id`. Поле `consultation_url` в AJAX не уходит, и callback сохраняет пустую строку.
1. Добавить в payload `consultation_url: $form.find('[name=consultation_url]').val()`.
2. В `templates/admin/components/tabs/settings-tabs/settings-7-config.php` перенести `.fs-field` ссылки из карточки «Тестовое окружение» в «Оформление» (под логотип).
3. Футер с кнопкой «Сохранить настройки» выводится только при `$otp['editable'] || $test_env['editable']`. Если оба заданы в `wp-config.php`, логотип и ссылку нельзя сохранить. Условие убрать: кнопка показывается всегда.
4. `ConfigCallbacks`: `esc_url_raw()` вызывается напрямую. Если в `Sanitizer` есть URL-метод — использовать его, иначе оставить.
5. Проверить: сохранить → перезагрузить → значение на месте; приглашение показывается в тренажёре.

### Шаг 4. Задача 3 — отчество в таблице «Ученики»
`templates/admin/components/tabs/userlist-tabs/userlist-2-students.php` (около стр. 180): `$person->fullName()` перетирается `$wpUser->display_name`, в котором отчества нет. У «Родителей» берётся `fullName()`.
1. Брать `fullName()`, а `display_name` использовать как фолбэк, только если `fullName()` пуст.
2. Так же поправить `$parentName` в той же таблице.
3. Проверить поиск по ФИО (`student_name`): он ищет по полям персоны, не по `display_name` — сортировка и фильтр не пострадают.

### Шаг 5. Задача 2 — ширина фильтров «Ученики» и «Родители» = 310px
1. В `src/scss/admin/components/_logs.scss` не менять общий `.fs-logs-filters`: его используют логи. Добавить модификатор `fs-logs-filters--wide` в формы `userlist-2-students.php` и `userlist-3-parents.php`.
2. Модификатор задаёт `width: $filter-control-width` для `input[type=search]` и для `render_fs_select` (проверить его разметку: кастомный селект или нативный `<select>`, ширина может ставиться на обёртку). Токен `$filter-control-width: rem(310)` завести в `admin/_variables.scss`.
3. Проверить перенос строк фильтров на узком экране (flex-wrap).

### Шаг 6. Задача 7 — выравнивание в «Лекции» (`src/scss/player/components/_step-text.scss`)
1. `.wpc p, .wpc li` — `text-align: justify` (без переносов; переносы при желании — как в шаге 9).
2. `.wpc img`: `display: block; margin-inline: auto`. Картинка внутри `<p>` становится блоком и центруется независимо от выключки абзаца.
3. Учесть классы WP `alignleft`/`alignright`: если автор выровнял явно, центр не навязывать (`img:not(.alignleft, .alignright)`). `aligncenter` уже совпадает с поведением по умолчанию.
4. Абзац, в котором только картинка или формула QuickLaTeX, не растягивать justify (для одной строки не влияет, проверить визуально).

### Шаг 7. Задача 6 — LaTeX в шаге «Лекция» теряет `\`
Гипотеза (проверить по БД): `LessonCallbacks::ajaxSaveLessonSteps()` → `unslashArray('steps')` → `LessonManager::saveMeta()` → `PostManager::updateMeta()` → `update_post_meta()`. Ядро вызывает `wp_unslash()` на значении ещё раз, и `\cdot` превращается в `cdot`.
1. Проверить: `SELECT meta_value FROM wp_postmeta WHERE post_id=<урок> AND meta_key='fs_lms_meta'` — есть ли `\\cdot` в сохранённом.
2. Исправить в одном месте — `PostManager::updateMeta()`: `update_post_meta( $post_id, $key, wp_slash( $value ) )`. Так же проверить `addMeta` и прочие обёртки мет в `PostManager`/`MetaBoxManager`/`UserManager`.
3. Правка глобальная. Прогнать всех вызывающих (`grep updateMeta`), нет ли где данных, которые уже слэшированы (сырой `$_POST` без unslash). По правилам проекта таких быть не должно. Это заодно чинит пункт 3 шага 2 и код Python в метах.
4. Уже сохранённые лекции не починятся сами: формулы нужно пересохранить вручную. Автоматическая миграция невозможна — потерянный `\` не восстановить.
5. Если в БД слэши на месте: искать на клиенте — `inline-editor.js` (как TinyMCE отдаёт контент, `registerBlockButtons(..., {latex:'mathjax'})`) и `sanitizeHtmlValue()` в `LessonAuthoringService::sanitizeStep()`.
6. Тест: сохранение шага с `$\cdot$` → чтение → `\cdot` на месте.

## Этап 3. Низкий приоритет

### Шаг 8. Задача 9 — мобильный тренажёр
1. Фильтры сверху. В `all-tasks-body.php` `<aside>` в DOM уже стоит первым, но `src/scss/frontend/components/_sidebar.scss:175` на `$bp-md` ставит `order: 2`. Проверить, какой класс у сайдбара тренажёра. Для тренажёра убрать `order: 2`; у страницы задания — оставить, если там это задумано.
2. На мобильном фильтры сверху занимают экран — свернуть их в `<details>`/кнопку «Фильтры». Уточнить у пользователя, нужно ли это. По умолчанию — просто наверх.
3. Кнопки в `.tcr-foot` (`_task-card.scss`, медиа `$bp-md`): `flex-direction: column; align-items: center`, `.tcr-actions { margin-left: 0 }`. Разметку PHP (`all-tasks-body.php`) и JS (`task-card.js`) не трогать — только CSS.

### Шаг 9. Задача 11 — мобильная статья, выключка по ширине и переносы
Переносы есть: `hyphens: auto` работает для русского, если у страницы `lang="ru-RU"` (проверить `<html lang>` темы).
1. `src/scss/frontend/components/article/_prose.scss`: в `@media (max-width: $bp-mobile)` для `.fs-article-prose p, li` — `text-align: justify; hyphens: auto;`.
2. Заголовки, код, таблицы, подписи не трогать.

### Шаг 10. Задача 10 — заголовок одного задания на мобильном
`src/scss/frontend/components/_task-content.scss:6`: сейчас на `$bp-sm` — `$font-size-md`, на десктопе — `$font-size-2xl` (28px).
Ступени рядом с 32px: `$font-size-2xl` = 28, `$font-size-3xl` = 36. Мобильный заголовок 32px будет **крупнее десктопного**. Уточнить у пользователя:
- вариант А: мобильный = `$font-size-2xl` (28px, как на десктопе), медиа-правило просто убрать;
- вариант Б: завести ступень `rem(32)` и поднять десктоп тоже.
По умолчанию — А.

## Финал
1. `npx gulp build`, `npm run ci` (PHPUnit, eslint, stylelint).
2. Ручная проверка на dev (`docker restart wp_app` после PHP-правок): тренажёр (десктоп и мобильный), страница задания, статья, шаг «Лекция», настройки, «Ученики»/«Родители», импорт 19.
3. Бамп версии и релиз — по запросу.

## Решения (2026-09-19)
- (8) Таблица `.fs-table-center` 90% — везде.
- (1) Первую строку без `<thead>` не красить — цвет только у настоящей шапки (`th`).
- (5) Подпись «Задание N» жирным — к каждому подпункту; между подпунктами в одной карточке — отступ, чтобы тексты не сливались.
- (9) Фильтры на мобильном сворачиваются под кнопку «Фильтры».
- (10) Мобильный заголовок = `$font-size-2xl` (28px, как на десктопе).

## Статус (2026-09-19)
Шаги 1–10 реализованы, `npm run ci` зелёный. Вживую не проверено: dev-контейнер `wp_app` смонтирован на отдельную копию плагина 1.0.4 (`FS-LMS/wordpress/wp-content/plugins/fs-lms`), а не на этот репозиторий.
Отклонения от плана:
- (5) Добавлен режим «Обновить уже импортированные задания» (чекбокс на странице переноса) — повторный импорт перезаполняет найденные по legacy_number задания вместо пропуска.
- (6) Уже сохранённые формулы (в dev-БД — и в уроках, и в заданиях, напр. пост 19472) починятся только пересохранением: срезанный `\` не восстановить.
- (9) Фильтры сворачиваются и на мобильном каталоге учебника — карточка фильтров общая (`partials/filters-side.php`).
