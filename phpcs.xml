<?xml version="1.0"?>
<ruleset name="WordPress Project">
    <description>Custom WP rules</description>

    <rule ref="WordPress"/>

    <!-- Что проверяем -->
    <file>.</file>

    <!-- Исключения -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/tests/*</exclude-pattern>
    <exclude-pattern>*.min.js</exclude-pattern>
    <exclude-pattern>*.min.css</exclude-pattern>
    <exclude-pattern>*.js</exclude-pattern>
    <exclude-pattern>*.scss</exclude-pattern>

    <!-- Стандарты WordPress -->
    <rule ref="WordPress">
        <!-- Можно ослабить конкретные правила -->
        <!-- <exclude name="WordPress.Files.FileName.InvalidClassFileName"/> -->

        <!-- Отключить проверку snake_case для методов -->
        <exclude name="WordPress.NamingConventions.ValidFunctionName.MethodNameInvalid"/>
        <exclude name="WordPress.NamingConventions.ValidFunctionName.FunctionNameInvalid"/>
        <exclude name="WordPress.NamingConventions.ValidVariableName.VariableNotSnakeCase"/>
        <exclude name="WordPress.Files.FileName.NotHyphenatedLowercase"/>
        <exclude name="Squiz.Commenting.InlineComment.InvalidEndChar"/>
        <exclude name="Squiz.Commenting.FunctionComment.ParamCommentFullStop"/>
        <exclude name="WordPress.Files.FileName.InvalidClassFileName"/>
        <exclude name="WordPress.Security.NonceVerification.Recommended: Processing form data without nonce verification."/>
    </rule>



    <!-- Дополнительные правила -->
    <rule ref="WordPress-Extra"/>

    <!-- Формат вывода -->
    <arg name="report" value="full"/>
    <arg name="colors"/>
    <arg value="ps"/>

    <!-- Минимальная версия WordPress -->
    <config name="minimum_supported_wp_version" value="5.8"/>

    <!-- Тип проекта -->
    <config name="testVersion" value="7.4-"/>
</ruleset>